- name: Install PowerDNS
  apt:
    name:
      - pdns-server
      - pdns-backend-pgsql
    state: present
    update_cache: true

- name: Enable and start PowerDNS
  systemd:
    name: pdns
    enabled: true
    state: started
