loki_endpoint: "http://10.10.10.2:3100"
loki_tenant_endpoint: "http://10.10.10.2:3101"
node_agent_binary_path: "{{ playbook_dir }}/../bin/node-agent"
dbadmin_proxy_binary_path: "{{ playbook_dir }}/../bin/dbadmin-proxy"
# DEV ONLY — regenerated by setup wizard for production
ssh_ca_public_key: "REPLACE_ME_run_setup_wizard"

# Node identity (previously set by Terraform cloud-init)
temporal_address: "10.10.10.2:7233"
region_id: "dev"
cluster_id: "vm-cluster-1"
controlplane_ip: "10.10.10.2"
core_api_url: "http://10.10.10.2:8090/api/v1"
core_api_token: "hst_dev_e2e_test_key_00000000"
node_id: "{{ inventory_hostname }}"

# Ceph — generate with: just gen-ceph-keys
# DEV ONLY — regenerated by setup wizard for production
ceph_fsid: "REPLACE_ME_run_just_gen-ceph-keys"
ceph_web_key: "REPLACE_ME_run_just_gen-ceph-keys"
storage_node_ip: "10.10.10.50"
