apiVersion: v2
name: hosting
description: Hosting platform control plane (core-api, worker, admin-ui)
type: application
version: 0.1.0
appVersion: "0.1.0"

# Optional dependencies — disabled by default.
# Enable for quick evaluation only; use managed services in production.
dependencies:
  - name: postgresql
    version: "~18"
    repository: https://charts.bitnami.com/bitnami
    condition: postgresql.enabled
  - name: temporal
    version: "~0.73"
    repository: https://go.temporal.io/helm-charts
    condition: temporal.server.enabled
